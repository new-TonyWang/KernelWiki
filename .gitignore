.codex
.humanize/
.claude/
__pycache__/
*.pyc
corpus/INDEX/
data/mcp_tokens*.sqlite*
# fetch_pr_diff.py staging dirs — renamed into place once complete, so a
# committed one is always a half-written bundle
artifacts/prs/*/.PR-*.new/
