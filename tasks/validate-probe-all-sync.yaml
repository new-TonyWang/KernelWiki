task_id: 2026-04-17-validate-probe-all-sync
task_type: probe-api
target_path: wiki/nvidia/api-definitions/runtime/

hardware:
  device: H200
  sm: "9.0a"
  cuda_runtime: "12.9"

upstream_scope:
  - <local-path>
  - <local-path>

references:
  - reasoning/api-probing.md
  - reasoning/benchmark-protocol.md

success_criteria:
  - file_exists: wiki/nvidia/api-definitions/runtime/__all_sync.md
  - source_corpus_used: true
  - final_report_mentions_probe: true

notes: |
  Validate the new source corpus integration by probing the runtime intrinsic
  `__all_sync`.

  Requirements:
  1. Use `source_search` and `source_read` to find the original semantics first.
  2. If the API is sufficiently documented, create or update
     `wiki/nvidia/api-definitions/runtime/__all_sync.md`.
  3. If you can complete the minimal runnable probe, also write the probe record
     under `sources/experience/api-probes/`.
  4. If the task cannot finish within the current turn budget, still report the
     retrieval hits you used and what remains blocked.
