# Deprecated branch

This `master` branch is deprecated.

Please use the `main` branch as the canonical/default branch for all future work:

```bash
git fetch wty
git switch main
```

Do not base new changes on `master` unless you are intentionally maintaining historical compatibility.
